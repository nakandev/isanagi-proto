//===-- {{ namespace }}Subtarget.h - Define Subtarget for the {{ namespace }} -*- C++ -*-===//

#ifndef LLVM_LIB_TARGET_{{ namespace.upper() }}_{{ namespace.upper() }}SUBTARGET_H
#define LLVM_LIB_TARGET_{{ namespace.upper() }}_{{ namespace.upper() }}SUBTARGET_H

#include "{{ namespace }}FrameLowering.h"
#include "{{ namespace }}ISelLowering.h"
#include "{{ namespace }}InstrInfo.h"
#include "{{ namespace }}RegisterInfo.h"
// #include "{{ namespace }}SelectionDAGInfo.h"
#include "llvm/CodeGen/SelectionDAGTargetInfo.h"
#include "llvm/CodeGen/TargetSubtargetInfo.h"
#include "llvm/IR/DataLayout.h"
#include "llvm/Target/TargetMachine.h"

#define GET_SUBTARGETINFO_HEADER
#include "{{ namespace }}GenSubtargetInfo.inc"

namespace llvm {
class StringRef;

class {{ namespace }}Subtarget : public {{ namespace }}GenSubtargetInfo {
  virtual void anchor();
  {{ namespace }}FrameLowering FrameLowering;
  {{ namespace }}InstrInfo InstrInfo;
  {{ namespace }}RegisterInfo RegInfo;
  {{ namespace }}TargetLowering TLInfo;
  // {{ namespace }}SelectionDAGInfo TSInfo;

protected:
  bool Has64Bit;
  bool HasBigEndian;

public:
  // This constructor initializes the data members to match that
  // of the specified triple.
  {{ namespace }}Subtarget(const Triple &TT, StringRef CPU, StringRef FS,
               const TargetMachine &TM);

  {{ namespace }}Subtarget &initializeSubtargetDependencies(StringRef CPU, StringRef FS);

  // ParseSubtargetFeatures - Parses features string setting specified
  // subtarget options.  Definition of function is auto generated by tblgen.
  void ParseSubtargetFeatures(StringRef CPU, StringRef TuneCPU, StringRef FS);

  const {{ namespace }}InstrInfo *getInstrInfo() const override { return &InstrInfo; }
  const {{ namespace }}FrameLowering *getFrameLowering() const override {
    return &FrameLowering;
  }
  const {{ namespace }}TargetLowering *getTargetLowering() const override {
    return &TLInfo;
  }
  // const {{ namespace }}SelectionDAGInfo *getSelectionDAGInfo() const override {
  //   return &TSInfo;
  // }
  const {{ namespace }}RegisterInfo *getRegisterInfo() const override {
    return &RegInfo;
  }

  MVT getXLenVT() const {
    return Has64Bit ? MVT::i64 : MVT::i32;
  }
  unsigned getXLen() const {
    return Has64Bit ? 64 : 32;
  }
};
} // End llvm namespace

#endif
